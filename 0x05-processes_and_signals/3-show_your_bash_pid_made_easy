#!/usr/bin/env bash
# This script displays the PID along with the process name
# of processes whose name contains the word "bash".

# Use pgrep to find processes with the name "bash" and display PID and process name
pgrep bash -l
